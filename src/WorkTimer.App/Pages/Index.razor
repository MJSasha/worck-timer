@page "/"

@code {
    [Inject]
    NavigationManager navigationManager { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();

        navigationManager.NavigateTo(Pages.Timer.GetUrl());
    }
}